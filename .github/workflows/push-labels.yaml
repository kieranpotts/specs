#
# Push changes made to .github/labels.json to rebuild
# this repository's labels. This keeps the GitHub labels
# configuration defined in code.
#
# https://github.com/EndBug/label-sync
#

name: Push Labels

on:
  push:
    paths: ['.github/labels.json']

permissions:
  issues: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          sparse-checkout: .github/labels.json

      - name: Push labels
        uses: EndBug/label-sync@v2
        with:
          config-file: .github/labels.json
          token: ${{ secrets.GITHUB_TOKEN }}
          delete-other-labels: true
